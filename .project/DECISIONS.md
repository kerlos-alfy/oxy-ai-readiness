# Decisions

Canonical, ongoing decision log starting at Phase 1. Phase 0/0.5 decisions (including the 5 ADRs) are recorded in `.project/08-Decision-Log.md` and `.project/adr/` (not duplicated here).

## Phase 1 — Repository Foundation — 2026-07-24

**Decision:** `RepositoryInterface` is an empty marker interface, not a contract with required methods.
**Context:** `docs/04-Folder-Structure.md` lists one shared `Contracts/RepositoryInterface.php` for five repositories (Options, Post, File, Transient, User) that each wrap a completely different native WordPress storage API with no meaningfully shared method signature.
**Rationale:** Forcing a common method surface (e.g. a generic `find()`/`save()`) across Options/Transient (key-value), Post (WP content), File (filesystem), and User (identity) would produce awkward, misleading abstractions. The marker interface still enables Service Container tagging/discovery, matching the tagging pattern in `docs/29-Developer-Guide.md`'s `ServiceProvider` example.
**Affects:** `app/Contracts/RepositoryInterface.php`, all five repositories, future repositories added in later phases.

**Decision:** The five foundation repositories wrap *native* WordPress storage (options, transients, posts, users, filesystem) — none of them touch a custom `oxy_*` table.
**Context:** The user's Phase 1 scope ("Repository Foundation... do not implement Phase 2 or later") excludes the database migration work that was Phase 2 in the originally proposed plan (`.project/06-Phase-Plan.md`).
**Rationale:** `docs/04-Folder-Structure.md`'s named repository list (`OptionsRepository`, `PostRepository`, `FileRepository`, `TransientRepository`, `UserRepository`) maps exactly onto WordPress-native storage — none of the five require a custom table to exist. This makes Repository Foundation genuinely independent of the migration system, honoring "do not skip phases" / "do not implement Phase 2" while still being a complete, useful, testable slice of the architecture.
**Affects:** Scope boundary for Phase 1; Phase 2 will add table-backed repositories once migrations exist.

**Decision:** `FileRepository` receives its base directory and (optionally) an already-resolved `WP_Filesystem_Base` instance via constructor injection, rather than resolving `wp_upload_dir()` / calling `WP_Filesystem()` internally by default.
**Context:** Needed a way to make filesystem-touching code unit-testable under Brain Monkey (which mocks functions, not the `$wp_filesystem` global object) without requiring a real WordPress bootstrap or touching the real disk.
**Rationale:** Constructor DI lets tests inject a Mockery mock of `WP_Filesystem_Base` directly — clean, explicit, no global-state manipulation in tests. Production wiring (a future `ServiceProvider`, Phase 2+) will call `wp_upload_dir()` and `WP_Filesystem()` once and inject the results.
**Affects:** `app/Repositories/FileRepository.php`, its test suite, and the future ServiceProvider that constructs it.

**Decision:** `FileRepository::resolvePath()` rejects *any* path segment equal to `..`, rather than resolving the path and checking whether the result stays inside the base directory.
**Context:** `docs/26-Security-Spec.md` requires canonical Path Validation; `docs/28-Testing-Strategy.md`'s File Security Testing explicitly lists "Relative Paths" and "Invalid Paths" as required test cases.
**Rationale:** `realpath()`-based resolve-then-check does not work reliably for files that do not exist yet (the write case) since `realpath()` returns `false` for non-existent paths. A blanket "no `..` segment, no absolute path, no null byte" rule is simpler, has no edge cases, and is easy to verify by inspection — preferred over a more permissive but harder-to-verify resolve-and-compare strategy.
**Affects:** `app/Repositories/FileRepository.php`.

**Decision:** `PostRepository::query()` throws `InvalidArgumentException` on `posts_per_page = -1` (WordPress's own "unbounded" convention) and clamps any larger value to 100.
**Context:** `docs/27-Performance-Spec.md`: "Avoid unbounded queries", "Maximum Rows Per Request 100 default".
**Rationale:** A repository is the correct place to enforce this system-wide rule once, rather than trusting every future caller to remember it.
**Affects:** `app/Repositories/PostRepository.php`.

**Decision:** Composer/tooling config (`composer.json`, `phpcs.xml`, `phpstan.neon`, `phpunit.xml.dist`) was created in Phase 1, even though Phase 0.5 had an explicit "do not create composer.json" restriction.
**Context:** That restriction was scoped explicitly to Phase 0.5 ("Do NOT write any production code. Do NOT create app/, assets/, composer.json or plugin files."). Phase 1's instructions are different: "Begin Phase 1... Implement only the Repository Foundation" — implementation is now in scope, and PSR-4-autoloaded PHP classes are not meaningfully "implementable" or checkable without a `composer.json`.
**Rationale:** Treated as necessary minimal scaffolding for the requested deliverable, not scope creep — deliberately excluded the plugin bootstrap file, `uninstall.php`, `readme.txt`, and `package.json` (frontend tooling), none of which the Repository Foundation needs.
**Affects:** Repo root. Flagged explicitly in `PHASE-1-REPORT.md` in case the user intended a narrower interpretation.

## Phase 1 validation pass — 2026-07-24 (real PHP/Composer/Node/npm/Git now available)

**Decision:** `phpcs.xml` was rewritten from a bare `WordPress-Extra` ruleset to a hybrid standard: PSR-12 governs formatting and naming; `WordPress.Security`, `WordPress.WP`, `WordPress.DB`, and `WordPressVIPMinimum.Security` are kept for WordPress-specific security/API-usage/i18n sniffs. Added `automattic/vipwpcs` to `require-dev` for the VIP-minimum sniffs.
**Context:** Running PHPCS for the first time (it had never actually executed before this session — no PHP was available) against the original bare-`WordPress-Extra` ruleset produced **1,877 errors across all 15 Phase 1 files**, 1,848 of them auto-fixable — almost entirely tabs-vs-spaces, snake_case-vs-camelCase variable/property naming, and K&R-vs-Allman brace placement. `docs/29-Developer-Guide.md`'s Coding Standards section lists both "WordPress Coding Standards" and "PSR-12 where compatible", and its Naming Conventions explicitly state Methods: camelCase, Typed Properties, Readonly properties — conventions that are structurally incompatible with WordPress-Extra's literal procedural-style formatting rules. The actual Phase 1 code was written consistently in the PSR-12/camelCase style the docs describe, not classic WordPress procedural style.
**Rationale:** This was surfaced to the user directly (`AskUserQuestion`) rather than resolved unilaterally, given the scale (touches every file) and the fact that either resolution — rewrite ~1,500 lines to snake_case/tabs, or narrow the ruleset — sets a binding precedent for every future phase's code style. The user chose the hybrid ruleset explicitly: "Use a hybrid PHPCS ruleset. Keep: PSR-12 as the coding style. WordPress-Extra security sniffs. WordPress.Security.*, WordPress.WP.*, WordPress.DB.*, WordPressVIPMinimum.Security.*. Exclude formatting and naming sniffs that conflict with PSR-12... Do not rewrite the codebase to procedural WordPress style."
**Affects:** `phpcs.xml`, `composer.json` (`automattic/vipwpcs` dependency), and the coding-standard precedent for every file written in every later phase — all future PHP code should target this hybrid standard, not literal `WordPress-Extra`.

**Decision:** Three narrow, path- or error-code-scoped PHPCS exclusions were added on top of the hybrid ruleset (not blanket sniff disables) to resolve the remaining 64 errors/2 warnings after the ruleset switch.
**Context:** After switching to the hybrid ruleset, three categories of finding remained, none of them real defects: (1) `PSR1.Methods.CamelCapsMethodName.NotCamelCaps` firing on every PHPUnit `test_snake_case_description` method name — the standard, intentional PHPUnit test-naming convention; (2) `PSR1.Classes.ClassDeclaration.{MissingNamespace,MultipleClasses}` and `Squiz.Classes.ValidClassName.NotCamelCaps` firing on `tests/stubs/wp-core-stubs.php`'s `WP_User`/`WP_Post`/`WP_Filesystem_Base` — these intentionally mirror real WordPress core's actual non-namespaced, snake_case-method global classes, not this project's own convention; (3) `WordPress.Security.EscapeOutput.ExceptionNotEscaped` firing on every `sprintf()` used to build an `InvalidArgumentException`/`RuntimeException` message — none of which are ever echoed to a browser, the context that sniff targets.
**Rationale:** Each exclusion is scoped as narrowly as PHPCS allows (by `exclude-pattern` to `tests/*` or `tests/stubs/*`, or by the sniff's own specific error code rather than its whole category) so real enforcement — camelCase methods in `app/`, proper namespacing for actual project classes, and genuine output-escaping (`OutputNotEscaped` etc.) — stays fully active everywhere else.
**Affects:** `phpcs.xml`. Two additional non-ruleset fixes: `RepositoryContractTest.php` (wrapped a 124-character line) and `PostRepositoryTest.php` (inline `phpcs:ignore` on a test that deliberately passes an out-of-range `posts_per_page` of 5000 to exercise the documented 100-row clamp).

**Decision:** `composer test` now runs `phpunit --no-coverage`; a separate `test:coverage` script (`phpunit`, no flag) was added for when a coverage driver is available.
**Context:** `phpunit.xml.dist` unconditionally requests HTML/text coverage reports. No Xdebug or PCOV driver is installed on the available PHP build (`C:\php`), so any plain `phpunit`/`composer test` run failed with a PHPUnit-runner warning ("No code coverage driver available") that turns the overall run's exit code non-zero even though all tests pass.
**Rationale:** Splitting "fast test run" from "coverage-enforced run" is standard practice and doesn't touch the coverage configuration itself (still present in `phpunit.xml.dist` for whenever a driver exists) — it just stops the default `test` script from failing over an environment capability gap rather than an actual test failure.
**Affects:** `composer.json`. **Flagged as an open risk, not resolved**: `docs/28-Testing-Strategy.md`'s coverage thresholds (80% minimum, 90–95% for core/security/scoring/auto-fix/REST) cannot be measured or gated until a coverage driver is installed — install `pcov` (faster) or `xdebug` (mode `coverage`) before trusting coverage numbers on any later phase, especially security/scoring/auto-fix work.

**Decision:** Fixed a real bug in `FileRepository::write()`, found by PHPStan (level 8), not invented scope creep.
**Context:** The original code called `$fs->mkdir($directory, 0755, true)`, modeled on PHP's native `mkdir($path, $mode, $recursive)` signature. `WP_Filesystem_Base::mkdir()`'s actual signature (confirmed against `php-stubs/wordpress-stubs`) is `mkdir($path, $chmod = false, $chown = false, $chgrp = false)` — there is no recursive-creation option at all. `true` was silently being passed as `$chown` (a user login name or `false`), and nested directories more than one level below the configured base would never actually have been created — a genuine, previously-undetected bug (undetectable by the manual review done before PHP was available, since it's a subtle real-vs-assumed-API-shape mismatch, not a syntax error).
**Rationale:** Added a private recursive `ensureDirectoryExists()` helper that walks from the target directory up to `$baseDir` (assumed pre-existing, consistent with the class's existing path-confinement design), creating each missing level individually via `$fs->mkdir($dir, 0755)`. This is a correctness fix within the existing design, not a redesign — `FileRepository`'s public API, `resolvePath()`'s confinement logic, and every other method are unchanged.
**Affects:** `app/Repositories/FileRepository.php`, `tests/Unit/Repositories/FileRepositoryTest.php` (updated the `mkdir` mock's expected argument list from 3 args to 2).

**Decision:** Added an empty `tests/Integration/` directory (`.gitkeep` placeholder only) rather than writing integration tests.
**Context:** `phpunit.xml.dist` declares both `Unit` and `Integration` testsuites, but only `tests/Unit/` existed — PHPUnit fails at configuration-parse time (before running anything) if any declared testsuite's directory is missing, which broke every invocation that didn't pass `--testsuite=Unit` explicitly, including the plain `phpunit`/`composer test` commands used for validation.
**Rationale:** Writing integration tests is out of scope for Phase 1 ("do not implement Phase 2 or later" / the user's explicit "do not start Phase 2" instruction this session) — the goal was only to make the existing config internally consistent, not to add new test coverage.
**Affects:** `tests/Integration/.gitkeep` (new, empty). `composer.json`'s existing `test:integration` script (`phpunit --testsuite=Integration`) will now run cleanly (0 tests) instead of erroring, once real integration tests are added in a later phase.

## Phase 2 — Foundational Scaffolding — 2026-07-25

**Decision:** Phase 2 is scoped to the deferred "Foundational Scaffolding" (bootstrap file, Container, `Core/`, activation lifecycle) rather than the originally-drafted Phase 2 ("Database & shared infrastructure": migrations, tables, Settings Manager, Logger, Cache Service, Queue).
**Context:** The draft plan (`06-Phase-Plan.md`) assumed its own Phase 1 (scaffolding) would already exist before its Phase 2 (DB infra) started. The user's actual, approved Phase 1 was narrowed to only the Repository layer, so the draft's Phase 2 had an unmet prerequisite — no Container/bootstrap exists yet for a migration runner or Settings Manager to register into, per `docs/02-Architecture.md`'s Bootstrap Sequence (Autoloader → Constants → Service Container → Register Services → Core Components → ...).
**Rationale:** Surfaced via `AskUserQuestion` rather than silently picking an interpretation, since either reading (bootstrap-only vs. bootstrap+DB-infra-combined) commits real, hard-to-cheaply-undo scope. User chose bootstrap/Container/Core only, explicitly excluding DB tables/migrations/Settings Manager/Logger/Cache Service/Queue.
**Affects:** Everything built this phase; `Core/Scheduler.php`, `Core/ModuleRegistry.php`, `Core/StandardsRegistry.php`, and all DB-infra work are deferred to a future phase, not this one.

**Decision:** `ServiceProvider` (abstract base class) lives at `app/Providers/ServiceProvider.php` (namespace `OxyAI\Providers`), not `app/Core/Container/ServiceProvider.php` (namespace `OxyAI\Core\Container`) as `docs/29-Developer-Guide.md`'s worked example literally imports it.
**Context:** That example's `use OxyAI\Core\Container\ServiceProvider;` requires `Container` to be a namespace (a folder), which conflicts with `docs/04-Folder-Structure.md`'s canonical Core/ list, where `Container.php` is a flat file (a class, not a folder) — PSR-4 cannot have a class and a namespace share one name.
**Rationale:** Treated the dev-guide's import as illustrative rather than literal, same precedent as Phase 1's `RepositoryInterface` marker-interface decision. Placed the base class at `app/Providers/ServiceProvider.php` instead, which matches the already-documented top-level `Providers/` folder in `docs/04-Folder-Structure.md`'s App structure section — no new folder invented, no naming collision.
**Affects:** `app/Providers/ServiceProvider.php`; the import path every future Core/Module service provider will use.

**Decision:** `Container::bind()`/`singleton()` take zero-argument factory closures (`Closure(): mixed`), not `Closure(Container $c): mixed` auto-wiring closures.
**Context:** Nothing bound into the Container this phase (currently just `Config::class`) needs the container to resolve its own dependencies — `Config` is constructed directly from plugin-file/version strings known at `Plugin::__construct()` time.
**Rationale:** Building auto-wiring (passing the container into every factory, or reflection-based constructor resolution) with no real consumer yet would be exactly the kind of speculative, undemonstrated feature the project's engineering guidelines warn against. Deferred until a later phase's Provider actually needs the container to resolve a factory's own dependencies.
**Affects:** `app/Core/Container.php`, `app/Core/Application.php`'s pass-through methods, and the signature every future `ServiceProvider::register()` call binds against — adding container-aware factories later is additive, not a breaking change.

**Decision:** `Bootstrap::run()` fires a new `oxy_ai_ready` action after marking the `Application` booted, matching the `oxy_ai_` prefix convention `OptionsRepository` already established, rather than inventing an unprefixed or differently-named hook.
**Context:** No hook-naming convention is specified anywhere in `docs/*` for the plugin's own lifecycle events (`docs/22-Plugin-SDK.md`'s "Hook System" only documents per-module before/after events like "Before Generation"/"After Generation"); `docs/29-Developer-Guide.md`'s Bootstrap Flow only names the step "Ready Event" without a literal hook name.
**Rationale:** Reused the one naming convention that does exist in the codebase (`OptionsRepository::PREFIX = 'oxy_ai_'`) rather than inventing a new, unrelated convention, so a later phase's naming-convention doc (if one gets written) has one existing precedent to reconcile against, not two.
**Affects:** `app/Core/Bootstrap.php`. Third-party code or later phases hooking into plugin readiness should use `oxy_ai_ready`.

**Decision:** `Plugin::activate()` uses `OptionsRepository` to record `installed_at` (once) and `version` (every activation); `Plugin::deactivate()` is an intentionally empty method body.
**Context:** CLAUDE.md's "do not store all data in WordPress options" restriction targets using `wp_options` as the general operational datastore (the whole `oxy_*` table schema); `OptionsRepository`'s own Phase 1 docblock explicitly names "installed version, activation timestamp" as its legitimate use case.
**Rationale:** This is that exact, already-approved use case, not new scope — no `oxy_*` table exists yet for this to belong to instead. `deactivate()` has a real, WordPress-required callback registered (`register_deactivation_hook`) but a genuinely empty body: no scheduled events, transients, or caches exist yet to clear. An empty-but-honestly-empty method is not the same as placeholder code claiming to do something it doesn't.
**Affects:** `app/Core/Plugin.php`.

**Decision:** Added a narrowly file-scoped PHPCS exclusion: `PSR1.Files.SideEffects.FoundWithSymbols`, scoped to `oxy-ai-readiness.php` only.
**Context:** The hybrid ruleset (PSR-12 base) flagged the plugin main file for both defining constants (`OXY_AI_READINESS_VERSION` etc.) and executing side effects (the `ABSPATH` guard, requiring the autoloader, registering activation hooks) in one file.
**Rationale:** Every WordPress plugin bootstrap file necessarily does both — that is what the file is for; there is no PSR-1-compliant way to write one. Scoped by `exclude-pattern` to that one filename only (same narrow-exclusion pattern as Phase 1's three), not a project-wide sniff disable.
**Affects:** `phpcs.xml`, `oxy-ai-readiness.php`.

**Decision:** Extended `phpstan.neon`'s `paths` and `phpcs.xml`'s `<file>` list to include the two new root plugin files (`oxy-ai-readiness.php`, `uninstall.php`), which previously only covered `app/`/`tests/`.
**Context:** Root-level plugin files didn't exist before this phase; static analysis/linting had nothing to scope to there.
**Rationale:** Keeping the "0 errors, 0 warnings" bar meaningful requires covering every hand-authored PHP file, not just `app/`.
**Affects:** `phpstan.neon`, `phpcs.xml`.

**Decision:** Fixed three PHPUnit "risky" (no-assertions) warnings by using Brain Monkey's real simulated hook storage (`Actions\has()`) where one exists, and `expectNotToPerformAssertions()` where only a Mockery expectation applies (`get_option`/`update_option`, which Brain Monkey does not simulate for real) — not by adding a hollow `self::assertTrue(true)`.
**Context:** `Plugin::run()`/`activate()`/`deactivate()` are void methods; tests that only set Mockery/Brain-Monkey expectations (verified in `tearDown()`) register zero assertions from PHPUnit's own point of view, so PHPUnit correctly flags them as risky.
**Rationale:** `expectNotToPerformAssertions()` is PHPUnit's documented mechanism for exactly this case — a test whose correctness is verified by a non-PHPUnit assertion library — and is more honest than a placeholder assertion that verifies nothing.
**Affects:** `tests/Unit/Core/PluginTest.php`.

## Phase 3 — Module & Standard SDK skeleton — 2026-07-25

**Decision:** Read "start Phase 3 exactly according to the roadmap" as: implement `06-Phase-Plan.md`'s row 3, "Module & Standard SDK skeleton," under our own actual phase numbering — not the draft's own Phase 2 ("Database & shared infrastructure"), even though the roadmap table lists row 3 as depending on row 2.
**Context:** Our actual Phase 2 was the deferred scaffolding (bootstrap/Container/Core) — the draft's own Phase 1, not its Phase 2. The draft's Phase 2 (DB infra) still doesn't exist. Two readings of "Phase 3" were possible: the draft's Phase 2 content (since that's next-in-sequence after our actual Phase 2), or the draft's literal row labeled "Phase 3." The user named "Phase 3" specifically and said "exactly according to the roadmap," which only resolves to one row in that document.
**Rationale:** Chose not to re-ask, since the user had already seen this exact ambiguity surfaced in `PHASE-2-REPORT.md` §8 and answered directly with a phase number rather than picking one of the two options offered there. Verified the choice doesn't produce placeholder work: the roadmap row's own exit criterion ("A module can be enabled/disabled at runtime without touching core; events fire on lifecycle transitions") doesn't require persistence, and the row itself scopes to "one minimal internal 'probe' module/standard for validation only (not user-facing)" — i.e., it's meant to be a skeleton, not a fully wired production system needing the DB layer.
**Affects:** Everything built this phase. `Core/Scheduler.php` and all DB-infra work (migrations, `oxy_settings`/`oxy_modules`/`oxy_standards` tables, Settings Manager, Logger, Cache Service, Queue) remain unbuilt and are candidates for whichever phase comes next.

**Decision:** `ModuleRegistry`/`StandardsRegistry` state (registered/enabled/booted) is in-memory only for this phase — no persistence to `oxy_modules`/`oxy_standards` (those tables don't exist).
**Context:** `docs/25-Database-Schema.md` defines `oxy_modules`/`oxy_standards` with a `status` column, implying persisted enable/disable state; that table doesn't exist yet (DB infra is a future phase).
**Rationale:** The Phase 3 roadmap row's exit criterion says "enabled/disabled at runtime," not "...persisted across requests" — in-memory state satisfies it honestly. Building a fake persistence layer now (e.g., silently falling back to `wp_options` for module state) would violate "do not store all data in WordPress options" and would need to be ripped out once the real `oxy_modules` table exists.
**Affects:** `app/Core/ModuleRegistry.php`, `app/Core/StandardsRegistry.php`. Flagged as a known gap for whoever builds the DB-infra phase: registry state must move to persisted storage then, not stay in-memory forever.

**Decision:** `ModuleInterface` and `StandardInterface` implement the full documented method lists from `docs/05-Modules.md`/`docs/22-Plugin-SDK.md` and `docs/23-AI-Standards-Layer.md`, not a narrowed subset — including methods (`assets()`, `routes()`, `settings()`, `permissions()`, `audit()` on Module; `discover()` through `report()` on Standard) whose backing subsystems (asset pipeline, REST router, Settings Manager, Audit Engine, Discovery/Validation/Generation/Scoring/Monitoring/Reporting engines) don't exist yet.
**Context:** Implementing these methods honestly for the only concrete implementations that exist this phase (`ProbeModule`/`ProbeStandard`) meant either fabricating fake return data (a form of placeholder code CLAUDE.md explicitly forbids marking as complete) or being honest about "nothing here yet."
**Rationale:** Chose honesty over narrowing the contract: `ProbeModule`'s five collection-returning methods return empty arrays (a probe module genuinely has no assets/routes/settings/permissions/audit rules — that is the textually correct answer, not a stub pretending otherwise), and `ProbeStandard`'s six delegate methods throw `ModuleException` (the owning module genuinely has no Discovery provider/Generator/Validator/ScoreProvider/Monitor/Reporter registered — throwing reports that loudly instead of silently returning wrong data). Narrowing the interfaces instead would have deviated from the documented contract without a doc conflict to justify it.
**Affects:** `app/Contracts/ModuleInterface.php`, `app/Contracts/StandardInterface.php`, `app/Modules/Probe/ProbeModule.php`, `app/Modules/Probe/ProbeStandard.php`. Every future real Module/Standard (Robots, LLMS, etc.) implements the same full contract; their `assets()`/`routes()`/etc. return real data once the corresponding subsystem exists, and their Standard delegate methods stop throwing once their owning Module actually registers a Generator/Validator/etc.

**Decision:** Reused `do_action`/Phase 2's proven event-firing pattern for the seven Module/Standard lifecycle events, rather than building a separate Event/Listener dispatcher class hierarchy.
**Context:** `docs/04-Folder-Structure.md` lists both `Events/` and `Listeners/` folders; `docs/22-Plugin-SDK.md`'s Event Bus and `docs/23-AI-Standards-Layer.md`'s Events section name specific event types (`ModuleEnabled`, `StandardRegistered`, etc.).
**Rationale:** WordPress's own hook system already is a working event bus, and `Bootstrap::run()` already proved firing a lifecycle event via `do_action('oxy_ai_ready', ...)` works and is testable (Phase 2). Building a second, parallel Laravel-style Event/Listener dispatcher on top would be exactly the kind of undemonstrated, speculative abstraction the project's engineering guidelines warn against — two systems doing the same job. The `Events/` DTOs still exist as typed value objects (matching the documented file list), just passed as `do_action`'s argument rather than dispatched through a bespoke bus. No `Listeners/` folder was created — nothing subscribes to these events yet.
**Affects:** `app/Events/*.php`, `app/Core/ModuleRegistry.php`, `app/Core/StandardsRegistry.php`.

**Decision:** Added `Plugin::boot()` — a one-line passthrough to `Kernel::boot()` — as new public API.
**Context:** Needed a way to prove, in a test, that `Plugin::run()` followed by WordPress firing `plugins_loaded` really does register and boot the probe module through the real Container/Registry chain (the Phase 3 exit criterion, demonstrated at runtime, not only unit-tested in isolation).
**Rationale:** Brain Monkey's simulated `add_action()`/`do_action()` (confirmed by reading `vendor/brain/monkey/inc/wp-hook-functions.php`) only record hook registration/firing for Mockery-style expectations — they do not actually invoke previously-registered callbacks. There is no way to exercise "WordPress fires `plugins_loaded` and Kernel::boot() runs" under this test stack without a direct call. `Plugin::boot()` is one line and mirrors the existing `run()` delegation, not a new abstraction.
**Affects:** `app/Core/Plugin.php`, `tests/Unit/Core/PluginTest.php`.

**Decision:** Removed `phpstan.neon`'s inherited `excludePaths: [app/Modules/*]` (present since Phase 1, before any Module existed).
**Context:** Running PHPStan after adding `app/Modules/Probe/*` still reported "no errors" with only 29 files analysed — the same count as before those 3 new files existed, meaning they were silently skipped, not actually clean.
**Rationale:** A "0 errors" result that quietly excludes the files just written is a false-clean result, not a real quality gate — caught by checking the analysed-file count, not just the pass/fail line. Removed the exclusion; re-ran to a genuine 0 errors across 32 files.
**Affects:** `phpstan.neon`. Flagged so future phases don't assume any lingering path exclusions are still appropriate once real code lands in previously-empty directories.

## Phase 4 — Discovery Engine — 2026-07-25

**Decision:** Treated `docs/14-Discovery-Engine.md`'s REST route list (`/discovery`, `/discovery/map`, `/discovery/resources`, `/discovery/modules`, `POST /discovery/scan`, `POST /discovery/reset`) as authoritative over `docs/24-REST-API-Spec.md`'s conflicting Discovery API section (`/discovery`, `/discovery/files`, `/discovery/resources`, `POST /discovery/run`), and implemented only the three routes both lists effectively agree exist (`/discovery`, `/discovery/resources`, plus `/discovery/map` from docs/14) — as GET only.
**Context:** These two documents disagree on Discovery's own route names, a conflict `.project/adr/ADR-003-rest-api-naming.md` never caught (it only verified route *prefixing*, explicitly noting "Discovery... were already correctly prefixed and are unchanged" — prefixing, not naming, was checked).
**Rationale:** `docs/14` is the dedicated, detailed engine spec written with full context of the concepts its own routes name (`map` ↔ "Discovery Map", `scan` ↔ the pipeline's own step names) — more likely to be the precise, intentional naming than `docs/24`'s birds-eye cross-reference table, which uses terms (`/discovery/files`, `POST /discovery/run`) that don't correspond to any concept `docs/14` itself defines. Implementing only GET routes follows directly from this phase's own exit criterion ("read-only, no writes") — building a POST route under either candidate name would contradict that criterion outright.
**Affects:** `routes/api.php`, `app/Http/Controllers/DiscoveryController.php`. Flagged for whoever eventually needs `POST /discovery/scan` (or `/run`): the name is still unresolved between the two docs and should be settled before that route is built, not assumed from this phase's silence on it.

**Decision:** `DiscoveryService`'s Discovery Map is built lazily (`map()`/`resources()` trigger `scan()` only if nothing has been scanned yet) rather than eagerly during `Bootstrap`/`CoreServiceProvider`.
**Context:** docs/27-Performance-Spec.md's "Incremental Discovery <100ms" target and CLAUDE.md's "do not run heavy operations during frontend requests" both push against scanning on every request regardless of whether anything asks for Discovery data.
**Rationale:** With only the Probe fixture registered, eager vs. lazy makes no real performance difference today — but the pattern matters once real Modules register real (network/filesystem-touching) Discovery providers in later phases. Lazy-scanning now means later phases inherit the right default instead of having to retrofit it.
**Affects:** `app/Services/DiscoveryService.php`.

**Decision:** No `POST /discovery/scan` or `/discovery/reset` REST route this phase; `DiscoveryService::reset()` exists but is not REST-exposed.
**Context:** docs/24-REST-API-Spec.md's API Security list requires CSRF/Nonce Validation, Rate Limiting, Request Signing, and Audit Logging on API endpoints generally; none of the shared infrastructure for rate limiting (needs a Cache/Transient-backed counter) or audit logging (needs a Logger service) exists yet — both deferred to a DB-infra phase.
**Rationale:** Building one unsecured POST endpoint as a special case would contradict "Never skip security, authorization, validation, tests or rollback requirements" and this phase's own "read-only, no writes" exit criterion. Deferring the mutating endpoint entirely, rather than shipping it without the security controls its own spec doc demands, is the safer call.
**Affects:** `app/Services/DiscoveryService.php`, `routes/api.php`. A future phase adding write-capable REST routes anywhere in the project should build the shared rate-limiting/audit-logging middleware once, not per-route.

**Decision:** `DiscoveryController::authorize()` gates every Discovery route behind WordPress's built-in `manage_options` capability, not the custom `manage_oxy`/`view_audit` capabilities `docs/26-Security-Spec.md` names.
**Context:** Those custom capabilities aren't registered anywhere — no Permissions/Roles system exists yet (that's part of a Settings/capability-registration phase that hasn't happened; `ModuleInterface::permissions()` returns `[]` for every module so far, Probe included).
**Rationale:** `manage_options` is the standard WordPress "site administrator" capability and is Secure-by-Default/Least-Privilege-compatible as an interim gate — it's stricter than doing nothing, and doesn't require inventing capability-registration machinery ahead of the phase that's actually supposed to build it.
**Affects:** `app/Http/Controllers/DiscoveryController.php`. Should be revisited (swapped for a real registered `view_audit`-equivalent capability) once a Permissions/capability-registration system exists.

**Decision:** `routes/api.php`'s `register_rest_route()` calls reference an instantiated `DiscoveryController` (`[$controller, 'index']`), not a class-string (`[DiscoveryController::class, 'index']`) as `docs/29-Developer-Guide.md`'s literal "Adding A REST Route" example shows.
**Context:** That doc's own example calls a non-static method (`index()`) via a class-string callable array, which is not valid PHP for non-static methods (fatal in modern PHP without an object). `DiscoveryController` also needs constructor-injected `DiscoveryService` — impossible without an instance.
**Rationale:** Same precedent as Phase 3's `ServiceProvider`-namespace decision: treated the doc's code sample as illustrative of the *route-registration shape*, not a literal API contract, since taking it literally would produce non-functional PHP and contradicts the same guide's own Dependency Injection section (constructor injection over static/service-locator patterns).
**Affects:** `routes/api.php`. Every future route file should instantiate its controller (resolving dependencies from the `Application`/`Container`) rather than pass a bare class-string.

**Decision:** `Hooks` is now bound into the Container as a singleton in `Plugin.php`'s constructor — the *same* instance also passed directly to `Kernel` — rather than letting `CoreServiceProvider` construct its own `new Hooks()`.
**Context:** `RestServiceProvider::boot()` needs to resolve the plugin's `Hooks` registrar from the Container to register `rest_api_init` through it (keeping the "every hook goes through `Hooks`" rule from Phase 2). If `CoreServiceProvider` bound a freshly-constructed `Hooks` instance instead, it would diverge from the instance `Kernel` already uses for `plugins_loaded` — two separate registrars with inconsistent bookkeeping.
**Rationale:** Caught before writing the wiring, not after a test failure: `Hooks` needed the same single-shared-instance treatment `Config` already gets in `Plugin`'s constructor (construct once, bind that exact closure-captured instance), not a per-provider `new Hooks()`.
**Affects:** `app/Core/Plugin.php`, `app/Core/RestServiceProvider.php`.

## Phase 5 — Validation Engine — 2026-07-25

**Decision:** Fixed `ProbeStandard::discover()`/`validate()` to delegate to the owning module for real, rather than continuing to throw as they did through the end of Phase 4.
**Context:** Phase 3's own `DECISIONS.md` entry explicitly predicted this: "their Standard delegate methods stop throwing once their owning Module actually registers a Generator/Validator/etc." Phase 4 gave `ProbeModule` a real `discover()` (via `DiscoveryInterface`) but never updated `ProbeStandard::discover()` to match — a staleness bug by the project's own stated standard, not new scope invented this phase.
**Rationale:** Building `ValidatorInterface`/`ValidationService` this phase made the same staleness undeniable for `validate()` too (the module now has a real validator), so both were fixed together rather than fixing one and leaving the other's already-flagged inconsistency in place. `ProbeStandard`'s constructor now takes the owning `ProbeModule` directly (not an interface — a Standard is owned by exactly one Module per ADR-001, so depending on its concrete type is architecturally fine, not a cross-module dependency).
**Affects:** `app/Modules/Probe/ProbeStandard.php`, `app/Modules/Probe/ProbeServiceProvider.php` (now passes `$module` into `ProbeStandard`'s constructor), `tests/Unit/Modules/Probe/ProbeStandardTest.php` (rewritten: `discover()`/`validate()` get real assertions instead of joining the throws-data-provider).

**Decision:** `ValidatorInterface::validate()` returns `ValidationResult` directly (including its own `executionTimeMs`, self-measured via `microtime()`), rather than the engine (`ValidationService`) measuring timing and wrapping a separate, timing-free outcome DTO.
**Context:** A cleaner separation (validator returns pure pass/fail + message; engine adds resourceId/validator-id/timing) would need a second DTO and `ValidationResult`'s `readonly` properties would prevent the engine from "adding" timing after the fact without reconstructing the object.
**Rationale:** With only the Probe fixture validator existing (a same-microsecond comparison), engine-measured vs. self-reported timing makes no practical difference yet, and one DTO is simpler than two. Flagged as a design point to revisit once a real, meaningfully-slow validator exists (a validator with a slow/unreliable clock could self-report misleading timing) — not treated as settled forever.
**Affects:** `app/Contracts/ValidatorInterface.php`, `app/DTO/ValidationResult.php`, every future validator implementation.

**Decision:** `ValidationStatus` is a native PHP 8.1 backed enum, not a bare string constant/union type.
**Context:** docs/16-Validation-Engine.md's "VALIDATION RESULTS" section is a fixed, closed set (PASS/WARNING/FAIL/INFO/SKIPPED/UNKNOWN); the Phase 5 exit criterion itself uses the word "deterministically."
**Rationale:** A backed enum makes an invalid status a compile-time/static-analysis error rather than a runtime typo risk, directly reinforcing "deterministic" — stronger than the plain-string convention used for `ModuleRegistry`'s enabled/disabled bookkeeping (which only has two states and no external vocabulary to typo).
**Affects:** `app/DTO/ValidationStatus.php`, `app/DTO/ValidationResult.php`, `app/Services/ValidationService.php`'s `match()` on status.

**Decision:** `POST /validation/run` was implemented this phase, unlike Phase 4's deliberate deferral of `POST /discovery/scan`.
**Context:** Phase 4 deferred its POST route specifically because the exit criterion said "read-only, no writes." Phase 5's exit criterion has no such restriction.
**Rationale:** Running a validator against an already-discovered resource is a pure computation — it doesn't write to WordPress, the filesystem, or a database, unlike a hypothetical Discovery "scan" that (in later phases, once real providers exist) could touch external resources. The same `manage_options`-gated, unsecured-against-rate-limiting posture already accepted for the GET routes applies equally here; there was no new security gap introduced by making this one a POST.
**Affects:** `routes/api.php`, `app/Http/Controllers/ValidationController.php`.

**Decision:** Fixed `composer.json`'s `analyse` script to permanently include `--memory-limit=512M`.
**Context:** `composer quality`/`composer analyse` (the actual documented commands) crashed with a PHPStan OOM error against the default 128M limit once this phase's codebase grew past 38 files. Phase 1's own report had identified this exact problem and worked around it with a manual CLI flag every session since — but never fixed the underlying script, so any future session or CI run using the documented `composer analyse` command (not knowing to append the flag) would have hit the same crash.
**Rationale:** A workaround repeated by hand every session, rather than fixed once at its source, isn't really fixed — caught by taking the "run all quality gates" instruction literally (via the actual `composer` scripts, not just ad hoc CLI flags) rather than continuing to paper over it.
**Affects:** `composer.json`.

## Phase 6 — Generation Engine — 2026-07-25

**Decision:** `GeneratorInterface` is kept to `id()`/`resourceId()`/`supports()`/`generate()`, deliberately narrower than `docs/17-Generation-Engine.md`'s literal "Every generator implements... supports() generate() validate() preview() publish() rollback() cache() version()" list. `GenerationService` (the engine) owns publish/rollback/cache/version instead.
**Context:** That same document's own Vision section says "Instead of allowing every module to implement its own generation logic, the Generation Engine provides one centralized framework." Putting publish/rollback/cache/version on the per-generator interface would be exactly the opposite: every future Generator (Robots, LLMS, etc.) would have to reimplement identical file-versioning/caching logic the engine exists to centralize.
**Rationale:** Same category of judgment call as Phase 3's `ModuleInterface` reconciliation and Phase 4's REST-route-naming conflict: when a document's literal method/route list contradicts its own stated architecture, the architecture (and the exit criterion, which only tests the *pipeline's* behavior, never asks whether a Generator implements `rollback()` itself) wins. `validate()` is also omitted from `GeneratorInterface` for the same reason — validation already has its own `ValidatorInterface`/`ValidationService` (Phase 5); a Generator doesn't validate itself, `GenerationService::publish()` validates the Generator's associated Discovery Map entry via the existing engine.
**Affects:** `app/Contracts/GeneratorInterface.php`, `app/Services/GenerationService.php`, every future Generator implementation.

**Decision:** Versioning is a simple two-slot scheme — the current published file plus exactly one `.previous` backup — not a full version history log.
**Context:** docs/17's own "VERSIONING" section describes a fuller model (Version, Timestamp, Checksum, Author, Source, Rollback Point per generation), and the roadmap's Phase 2 draft row ("Database & shared infrastructure") is where a real version-history table would belong.
**Rationale:** The Phase 6 exit criterion says "rollback restores prior version" (singular) — a two-slot scheme satisfies that exactly, without needing an `oxy_*` table that doesn't exist yet. Building a fuller version log now would mean storing history somewhere it can't yet live (not in `wp_options`, not in a nonexistent custom table) — deferred until a DB-infra phase gives it a real home.
**Affects:** `app/Services/GenerationService.php`.

**Decision:** `POST /generation/rollback` was added to `routes/api.php` even though it doesn't appear in `docs/17-Generation-Engine.md`'s own REST API list (which has `POST /generation/reset` instead, a different operation — clearing cache/state, not restoring a prior version).
**Context:** The Phase 6 exit criterion explicitly requires rollback capability ("rollback restores prior version"). Without a REST route, `GenerationService::rollback()` would only be reachable from PHP code, never from outside the plugin — inconsistent with docs/24-REST-API-Spec.md's own Success Criteria ("No dashboard functionality should exist without a corresponding API endpoint").
**Rationale:** Rather than overload `/generation/reset` (a different, undocumented-here operation) or silently omit rollback from REST entirely, added a correctly-named route for what the exit criterion actually needs. Flagged here so a future phase reconciling REST naming project-wide treats this as a deliberate addition, not an oversight to "fix" by removing it.
**Affects:** `routes/api.php`, `app/Http/Controllers/GenerationController.php`.

**Decision:** Generated output is stored under `{pluginDir}/storage/generated/`, and `Plugin::activate()` now calls `wp_mkdir_p()` to ensure that directory exists.
**Context:** `docs/04-Folder-Structure.md` names `storage/generated/` explicitly as the plugin-root location for generated files. `FileRepository::write()` (Phase 1) only creates directories *below* its configured base directory via its private `ensureDirectoryExists()` — it stops recursion and assumes the base directory itself already exists. Without an explicit fix, every `GenerationService::publish()` call would fail on a fresh install, since nothing before this phase ever created `storage/generated/`.
**Rationale:** This is a genuine correctness gap introduced by this phase (no prior phase wrote to the filesystem in a way that needed a pre-existing directory), caught by tracing through what actually happens on a fresh install rather than only testing against the sandbox's already-implicitly-real `/base` test paths. `wp_mkdir_p()` (WordPress core, recursive) is the standard, idiomatic fix — cheaper and more correct than adding base-directory creation to `FileRepository` itself, which would change Phase 1's already-shipped, already-tested class for a concern specific to this one caller.
**Affects:** `app/Core/Plugin.php`, `app/Core/CoreServiceProvider.php` (constructs `FileRepository` with this path).

**Decision:** Added `tests/Unit/Support/InMemoryFilesystem.php`, a small in-memory `WP_Filesystem_Base` test double, rather than continuing to expect every individual filesystem call via Mockery (the pattern Phase 1's `FileRepositoryTest` uses).
**Context:** `GenerationService::publish()`/`rollback()` each involve a multi-step, data-dependent sequence of `FileRepository` reads/writes/moves (check existing content, conditionally back it up, write new content, later read it back for checksum/rollback) — pinning every call with `Mockery::shouldReceive(...)->once()->with(...)` would be extremely brittle against the exact sequence and would break on any minor internal refactor of `GenerationService`, even when its externally-visible behavior stays correct.
**Rationale:** A real (if trivial) in-memory implementation lets tests assert on *outcomes* (what's actually readable at a path after a sequence of operations) rather than *call sequences* — closer to genuinely testing behavior. Placed under `tests/Unit/Support/`, not `tests/stubs/`, since it's test-double behavior specific to this project's tests, not a mirror of real WordPress core class internals (the distinction `tests/stubs/wp-core-stubs.php`'s own docblock already draws).
**Affects:** `tests/Unit/Support/InMemoryFilesystem.php`, `tests/Unit/Services/GenerationServiceTest.php`, `tests/Unit/Http/Controllers/GenerationControllerTest.php`, `tests/Unit/Routes/ApiRoutesTest.php`.

## Phase 7 — Scoring Engine — 2026-07-25

**Decision:** Score weighting uses `ValidationResult::status` (Pass=1.0/Warning=0.5/Fail=0.0), not `docs/15-Scoring-Engine.md`'s documented Critical/High/Medium/Low/Info severity weights (20/10/5/2/0).
**Context:** Those severity weights assume rule results carry a severity — a concept that belongs to the Audit Engine's rule objects (`docs/06-Audit-Engine.md`), which doesn't exist yet. `ValidationResult` (Phase 5) was scoped narrowly to PASS/WARN/FAIL/etc. status, with no severity field, and retrofitting one now — with no real caller populating it meaningfully — would be inventing data to satisfy a formula rather than scoring what's actually known.
**Rationale:** The exit criterion only requires the calculation be deterministic and the grade boundaries correct, not that severity-weighting specifically be implemented this phase. Status-based weighting is honest about what data exists (Phase 5's Validation Engine), still genuinely "weighted" (different statuses count differently), and doesn't block a future phase from adding severity-aware weighting once the Audit Engine gives rule results a severity to weight by.
**Affects:** `app/Services/ScoringService.php`. A future Audit Engine phase should extend (not replace) this weighting once severity exists — e.g. blend severity and status, or let severity gate which results even count.

**Decision:** No `ScoreProviderInterface`/per-module score-provider registration was built, unlike Discovery/Validation/Generation's registry pattern. `ScoringService::calculate()` takes a plain array of `ValidationResult` supplied by the caller (`ScoreController`), and `ProbeStandard::score()` still throws.
**Context:** `docs/22-Plugin-SDK.md` lists a `ScoreProviderInterface` among its SDK Interfaces, and `docs/23-AI-Standards-Layer.md` describes a Standard's `score()` delegating to "the owning Module's already-registered... ScoreProvider," matching the pattern already used for Discovery/Validator/Generator.
**Rationale:** Unlike Discovery/Validation/Generation, Scoring doesn't need per-module registration to do its job — it's a pure aggregation over whatever `ValidationResult`s it's handed, and nothing about "how to score a set of results" varies per module the way "how to discover/validate/generate a specific resource" does. Inventing a `ScoreProviderInterface` now, with no real per-module scoring logic to justify it (Probe's own score would just be "run the same formula on my own results," identical to the engine's own default), would be a speculative interface with no genuine second implementation ever exercising it — the same category of premature abstraction the project's engineering guidelines warn against.
**Affects:** `app/Services/ScoringService.php`, `app/Modules/Probe/ProbeStandard.php` (`score()` unchanged, still throws). Revisit once the Audit Engine gives per-module/per-category scores a real reason to differ.

**Decision:** Added an inline `// phpcs:ignore PHPCompatibility.Variables.ForbiddenThisUseContexts.OutsideObjectContext` on `Grade::label()`'s `match ($this)` line, rather than a project-wide or per-file exclusion.
**Context:** The installed `PHPCompatibilityWP` sniff set flagged `$this` inside `match ($this) { self::APlus, ... }` as "can no longer be used in a plain function or method since PHP 7.1" — a false positive: this is a PHP 8.1 enum method, where `$this` legitimately refers to the enum case instance, not a plain function/method the sniff's older assumption is guarding against.
**Rationale:** Scoped to the single line that triggers it, consistent with every other PHPCS exclusion so far being as narrow as possible. If future enum methods hit the same false positive repeatedly, that's the signal to promote this to a scoped `exclude-pattern` in `phpcs.xml` instead — not before.
**Affects:** `app/DTO/Grade.php`.

## Phase 8 — First end-to-end module: Robots — 2026-07-25

**Decision:** Scoped the Robots module to only what the Phase 8 exit criterion requires (`/robots/*` REST, a snapshot test, an audit rule reaching Scoring output) — not `docs/07-Robots-Spec.md`'s full feature set (visual builder, version history with restore, auto-backup, third-party SEO-plugin merge/conflict detection, multi-format import/export, admin UI).
**Context:** The roadmap row itself frames this phase as "proving the whole pipeline on one concrete, well-specified feature before parallelizing" — a vertical-slice proof, not a request to build the entire enterprise-grade Robots Module in one phase.
**Rationale:** Same discipline as every prior phase: build against the exit criterion and the architecture, not the full aspirational doc. The deferred features all need infrastructure that doesn't exist yet — persisted settings/version storage (no DB-infra phase has run) and the Admin UI (a later phase) for the visual builder specifically. Building fake versions of them now (e.g., an in-memory-only "version history" that vanishes every request) would be worse than not building them, since it would look complete without being usable.
**Affects:** `app/Modules/Robots/*`. Full version history, backups, and third-party plugin merge detection remain open work for whenever the DB-infra and Admin UI phases exist.

**Decision:** The Robots module's default rule set is fixed/hardcoded (WordPress's own standard `/wp-admin/` disallow, `admin-ajax.php` explicitly allowed, plus docs/07's own "Allow AI" template for the five named AI crawlers) — not user-customizable this phase.
**Context:** Real rule customization (a user adding/editing/removing rules through the eventual visual builder) needs somewhere to persist those custom rules across requests. No Settings Manager or `oxy_*` table exists yet.
**Rationale:** This is real, correct, deterministic content — not mock/placeholder data (`/wp-admin/` disallow is WordPress's own actual default virtual robots.txt behavior; the AI-crawler allow rules are exactly the "Allow AI" template docs/07 itself documents) — genuinely useful, genuinely testable via the exit criterion's snapshot test, and honest about not yet being end-user-configurable.
**Affects:** `app/Modules/Robots/RobotsModule.php`. Becomes configurable once a Settings/DB-persistence phase exists to store the customized rules.

**Decision:** `POST /robots/reset` maps to `GenerationService::rollback()` (the existing two-slot current/previous mechanism from Phase 6), not the fuller version-history restore `docs/07-Robots-Spec.md` describes (`DELETE /robots/version/{id}`, arbitrary-version restore).
**Context:** docs/07 lists `/robots/reset` without much detail on its exact semantics, separately from the fuller `/robots/history`/`/robots/restore`/`DELETE /robots/version/{id}` version-management routes (which need persisted version history this project doesn't have).
**Rationale:** Reusing the existing, already-tested rollback mechanism gives `/robots/reset` genuine, working behavior now rather than either faking multi-version history or leaving the route unimplemented. Consistent with Phase 6's own `/generation/rollback` precedent (exposing real engine capability under a REST name, rather than building fake depth).
**Affects:** `app/Http/Controllers/RobotsController.php`.

**Decision:** `RobotsController` is a thin facade with no generation/validation logic of its own — every method delegates to the same `DiscoveryService`/`ValidationService`/`GenerationService` singletons the generic `/discovery`, `/validation`, `/generation` controllers already use.
**Context:** The roadmap row explicitly frames this phase as proof that the shared engines "generalize" beyond the internal Probe module to a real feature.
**Rationale:** Duplicating engine logic inside `RobotsController` (e.g., re-implementing publish/rollback there) would defeat the entire point of building centralized engines in Phases 4–7 — the Robots-specific REST surface exists only to expose robots-domain-shaped routes and parameter names, not to reimplement anything.
**Affects:** `app/Http/Controllers/RobotsController.php`.

## Phase 9 — Audit Engine orchestration — 2026-07-25

**Decision:** No new "Rule" interface/abstraction was built distinct from the existing `ValidatorInterface`/`ValidationService`. `AuditService::scan()` treats every registered Validator as the "rule" docs/06-Audit-Engine.md describes.
**Context:** docs/06's "Rule Engine" section says "Every check is a Rule... independent... returns PASS/WARNING/FAIL/INFO/SKIPPED" — a description that already matches `ValidatorInterface`/`ValidationResult` (Phase 5) exactly, field for field.
**Rationale:** Building a second `RuleInterface` alongside the already-existing `ValidatorInterface` would be two interfaces doing the identical job — the same category of premature-abstraction risk Phase 7 avoided by not building a `ScoreProviderInterface`. A "rule" in this codebase already is a registered Validator; the Audit Engine's actual job this phase is orchestration (tie Discovery+Validation+Scoring together into one report), not re-defining what a rule is.
**Affects:** `app/Services/AuditService.php`. If a future Audit Engine enhancement needs metadata a plain Validator can't carry (docs/06's fuller Rule Object: Priority, Estimated Fix Time, Estimated AI Impact, Dependencies), that's the point to introduce a richer rule wrapper — not before a real use needs it.

**Decision:** `ScanType::Deep` and `ScanType::Developer` are real, selectable enum cases that currently behave identically to `Full` (both force a fresh Discovery pass and validate everything found).
**Context:** docs/06 describes Deep as "Includes API, Performance, Security, Headers, Future Standards" and Developer as "Verbose, JSON Output, Logs" — but no Headers/Performance/Security-specific scanner modules exist yet (those are later-phase Modules), and no Logger service exists yet for "verbose logs."
**Rationale:** Rather than omit these two documented scan types entirely (which would mean changing `AuditService`'s public API again once those scanners exist) or fabricate fake differentiated behavior now, they're included as real values with honest, currently-identical-to-Full behavior. `ScanType::Quick` is the one genuinely different mode already implementable (skips forcing a fresh Discovery pass), which is the actual behavioral distinction this phase's engine set can support.
**Affects:** `app/DTO/ScanType.php`, `app/Services/AuditService.php`. Future Header/Performance/Security modules should register conditionally based on `ScanType` once that distinction has real scanners to gate.

**Decision:** `AuditController` does not implement `/audit/fix` or `/audit/verify` (both named in docs/06's own REST API list).
**Context:** Those two routes are the AutoFix Engine's REST surface (docs/06's own "Auto Fix Engine" section: Issue → Validate → Backup → Apply Fix → Verify → Success workflow) — a distinct engine the roadmap places in a later phase (10).
**Rationale:** Consistent with every prior phase's discipline: build the routes the current phase's exit criterion needs (`Full/Quick scan... returns a structured report`), not every route a doc lists for a different, not-yet-built engine.
**Affects:** `app/Http/Controllers/AuditController.php`, `routes/api.php`.

## Phase 10 — Recommendation + Auto Fix Engines — 2026-07-25

**Decision:** `AutoFixService::fix()` reuses `GenerationService`'s existing backup-then-write pipeline (Phase 6) as the Backup/Execute/Validate stages of docs/18-AutoFix-Engine.md's Fix Pipeline, rather than building a separate, parallel backup/versioning mechanism for AutoFix specifically.
**Context:** docs/18 describes its own Backup System (Create Snapshot, Store Version, Checksum, Timestamp, Rollback Point) as though AutoFix needs its own — but this is functionally identical to what `GenerationService::publish()` already does (back up existing content to a `.previous` file, refuse to write on validation failure).
**Rationale:** Building a second backup/versioning system for AutoFix specifically would duplicate Phase 6's already-tested, already-correct mechanism for no benefit — the "issue" AutoFix resolves, in this codebase, is exactly "a resource failed validation," and the "fix" is exactly "regenerate and republish it," which is already `GenerationService`'s job. AutoFix's real, non-duplicative contribution is the explicit post-write **Verify** step and the confirmation-tier gating — those are what's actually new this phase.
**Affects:** `app/Services/AutoFixService.php`. Every future fix strategy that isn't "regenerate a resource via its registered Generator" (e.g. "repair a broken link," docs/18's other Supported Fixes) will need its own backup/rollback mechanism when it's built — this phase only covers the regenerate-and-republish case, since that's the only fix strategy any registered Generator (Probe, Robots) currently implements.

**Decision:** `AutoFixService`'s post-fix Verify step is a second, independent `ValidationService::validate()` call against the resource *after* `GenerationService::publish()` has already written it — not a reuse of the same validation result `publish()` itself already checked before writing.
**Context:** `GenerationService::publish()` already validates the resource *before* writing and refuses to write on FAIL — so by the time `AutoFixService` gets a successful `GenerationResult` back, that same resource, by construction, already passed validation once.
**Rationale:** Docs/18's own pipeline lists Validate and Verify as two separate stages ("Execute → Validate → Verify"), and Verify's own description ("Run Validation ↓ Run Audit ↓ Compare Previous State ↓ Confirm Success") implies checking again *after* the change, not trusting the pre-write check alone — the pre-write and post-write states could differ in principle (e.g. a resource's underlying data changing between the two checks). Implemented as calling validate() again rather than trusting the first result, so the rollback path is real and exercisable (proven by `AutoFixServiceTest`'s Mockery validator returning PASS then FAIL across the two calls) — not dead code that could never actually trigger.
**Affects:** `app/Services/AutoFixService.php`, its test suite.

**Decision:** `FixTier::Confirmation`/`FixTier::Developer` require an explicit `$confirmed = true` argument to `AutoFixService::fix()`; there is no real "pending confirmation" workflow (persisted pending state, a second REST call to approve it) — the caller must already know it's confirmed and pass that in the same call.
**Context:** docs/18 describes Confirmation-tier fixes as needing real user interaction (docs/22-Plugin-SDK's aspirational Admin UI dashboard), which doesn't exist yet (Admin UI is Phase 12).
**Rationale:** A real two-step "propose, then separately confirm" flow would need somewhere to persist the pending proposal between requests (no `oxy_*` table exists) and a UI to display it. Modeling confirmation as an explicit boolean argument this phase is honest about what's actually enforced (the tier gates *execution*, correctly) without pretending a full workflow exists.
**Affects:** `app/Services/AutoFixService.php`, `app/Http/Controllers/AutoFixController.php` (`confirmed` REST param). A future Admin-UI-and-DB phase should replace this with real persisted pending-fix state.

**Decision:** Scoped the AutoFix rollback test suite to docs/28-Testing-Strategy.md's AUTOFIX TESTING/ROLLBACK TESTING sections' *testable subset* — Backup Creation, Execution, Validation, Verification, Success Report, and rollback specifically after Validation Failure and Filesystem Failure. Database Failure, Timeout, Permission Change, Dependency Conflict, Interrupted Request, and Partial Batch Execution are not tested.
**Context:** The Phase 10 exit criterion explicitly names "28's AutoFix Safety Tests" (no exact heading of that name exists in docs/28; the AUTOFIX TESTING + ROLLBACK TESTING sections are the closest, most directly applicable match).
**Rationale:** The six untested rollback scenarios each need infrastructure this project hasn't built: a database (for Database Failure — there are no `oxy_*` tables yet), an async/network layer (Timeout, Interrupted Request), a capability-registration system (Permission Change), a module-dependency graph (Dependency Conflict), and a batch-fix feature (Partial Batch Execution). Testing them now would mean fabricating fake infrastructure just to exercise a test, which is backwards — the tests should follow the infrastructure, not the other way around.
**Affects:** `tests/Unit/Services/AutoFixServiceTest.php`. Each of the six deferred scenarios should get a real test once its corresponding infrastructure exists (a DB-infra phase, a capability-registration phase, a batch-fix feature, etc.) — not invented speculatively now.

**Decision:** "Update Score" (a documented Fix Pipeline stage in docs/18) is not implemented inside `AutoFixService::fix()` — recomputing the score after a fix is left to the caller (e.g., re-running `AuditService::scan()`).
**Context:** `ScoringService::calculate()` needs the *full* set of `ValidationResult`s across every discovered resource to produce a meaningful site-wide score; `AutoFixService::fix()` only knows about the one resource it just fixed.
**Rationale:** Baking a full-site score recalculation into a single-resource fix operation would conflate two different concerns and make `AutoFixService` depend on gathering results for resources it has nothing to do with. Re-auditing after a fix is a reasonable, explicit follow-up action for whichever caller wants an updated score — not something `fix()` should do implicitly on every call.
**Affects:** `app/Services/AutoFixService.php`. `AuditController`/`RobotsController` callers wanting an updated score after a fix should call `POST /audit/start` afterward.

## Phase 11 — Remaining Discovery-pillar modules — 2026-07-25

**Decision:** Headers has no `HeadersStandard` class — unlike LLMS/Markdown/Content Signals/Robots, `HeadersServiceProvider` registers the module with `ModuleRegistry`/`DiscoveryService`/`ValidationService`/`GenerationService` but never touches `StandardsRegistry`.
**Context:** ADR-001's Module→Standard ownership table explicitly lists Headers among the modules that own no Standard ("No Standard: Dashboard, Audit, Headers, Settings, Logs, License, Updater, Commerce, Analytics, Monitoring, Reports").
**Rationale:** An initial `HeadersStandard.php` was written by mirroring the Robots/LLMS shape mechanically, then removed once cross-checked against the canonical ownership table in `.project/09-Canonical-Architecture.md` — a real, caught-before-shipping mistake, not a hypothetical. `HeadersServiceProviderTest` includes an explicit assertion that no Standard gets registered, so this stays enforced rather than silently regressing if someone "helpfully" adds one back later by copying another module's provider.
**Affects:** `app/Modules/Headers/HeadersServiceProvider.php` (no `StandardsRegistry` call), `tests/Unit/Modules/Headers/HeadersServiceProviderTest.php` (explicit negative assertion).

**Decision:** Headers' and Markdown's "generated resource" is a text declaration (HTTP header name:value pairs; a negotiation capability statement) rather than either a live HTTP mechanism (`send_headers` hook) or fabricated per-page content.
**Context:** Real Headers emission needs a live request context to attach headers to; real Markdown needs actual WordPress post/page content to convert from HTML. Neither exists in this project yet — there's no real WordPress install, and "do not use mock production data" rules out fabricating fake pages to convert.
**Rationale:** Representing both as generated text resources keeps every Phase 8/11 module structurally uniform (one `GeneratorInterface`, one file in `storage/generated/`, one snapshot test) rather than making two modules special cases with a different underlying mechanism. Both declarations are honest about what they are: Headers' is a real, correct header declaration that a future phase would wire to an actual `send_headers` callback; Markdown's is a real capability statement, not invented page content.
**Affects:** `app/Modules/Headers/HeadersModule.php`, `app/Modules/Markdown/MarkdownModule.php`. A future phase giving Headers real runtime effect should add a `send_headers` hook that reads this same generated declaration, not replace the generation mechanism.

**Decision:** Content Signals' `StandardInterface::specification()` returns a plain identifier string (`'content-signals/1.0'`), not an external URL — unlike Robots (robotstxt.org), LLMS (llmstxt.org), and Markdown (MDN's content-negotiation page), which do link to a real, confidently-known canonical spec page.
**Context:** Content Signals (as docs/11 describes it) is a newer, less universally standardized concept than the other three; a specific canonical URL for it wasn't confidently known.
**Rationale:** Per the standing instruction never to guess or fabricate a URL without confidence it's real, an internal identifier (matching the pattern already used for `ProbeStandard::specification()`, `'internal-probe/0.1.0'`) is the honest choice here — a wrong or unverifiable URL would actively mislead, where a plain descriptive string doesn't claim more than it can back up.
**Affects:** `app/Modules/ContentSignals/ContentSignalsStandard.php`.

## Phase 12 — Admin UI shell — 2026-07-25

**Decision:** `app/Admin/AdminServiceProvider.php` registers exactly one top-level wp-admin menu page whose entire body is an empty `#oxy-ai-readiness-root` mount node — not docs/04-Folder-Structure.md's aspirational `Admin/{Dashboard,Pages,Settings,Widgets,Components,Assets,Views,Controllers,Middleware}` per-page breakdown.
**Context:** That folder breakdown describes per-page server-rendered PHP views. The same document explicitly states (line 271) "There is no per-module Views/ or Assets/ folder. The admin UI is a single centralized React SPA" — a direct contradiction of its own earlier breakdown, which predates the SPA note.
**Rationale:** The React SPA (already built by a prior session before this phase resumed — see below) already owns all in-page navigation between Dashboard/Audit/module screens via `App.tsx`/`Sidebar.tsx`'s own client-side routing. Building out PHP `Pages/`/`Views/`/`Controllers/`/`Middleware/` folders that server-render screens the SPA already renders would duplicate the SPA's own job and contradict the doc's own explicit SPA note. Same precedent as every prior phase's documented deviation from an internally-contradictory or aspirational doc section (RestServiceProvider's namespace, Headers owning no Standard).
**Affects:** `app/Admin/AdminServiceProvider.php` only. If a genuine need for server-rendered admin sub-pages ever arises (e.g., a page needing PHP-only data unavailable via REST), that's the point to add `Admin/Pages/` — not before.

**Decision:** `AdminServiceProvider` accepts an optional constructor-injected `FileRepository` (`?FileRepository $distRepository = null`, defaulting to one built from `Config::pluginDir() . 'dist'` in `register()`), rather than reading the Vite manifest through a raw `global $wp_filesystem`.
**Context:** A first draft read the manifest by setting `global $wp_filesystem` directly inside the test suite to inject a fake filesystem. PHPCS's `WordPress.WP.GlobalVariablesOverride.Prohibited` sniff correctly rejected this — WordPress globals are meant to be read, not reassigned, even in tests.
**Rationale:** Constructor injection is also just more consistent with how the rest of this codebase already tests `FileRepository`-backed code (`GenerationServiceTest`, `RobotsControllerTest` — both construct `FileRepository` with an injected `InMemoryFilesystem` test double rather than touching globals). No other `ServiceProvider` in this codebase takes constructor arguments beyond `Application`; this is a narrow, test-only exception, not a new pattern for Providers generally.
**Affects:** `app/Admin/AdminServiceProvider.php`, `tests/Unit/Admin/AdminServiceProviderTest.php`.

**Decision:** Fixed two real bugs in the frontend tooling scaffold rather than working around them: bumped `eslint-plugin-react-hooks` from `^4.6.2` to `^5.0.0` (its old peer-dependency range, `eslint@^3–8`, conflicted with the already-pinned `eslint@^9.9.0` and made `npm install` fail outright with `ERESOLVE`), and disabled the `no-undef` ESLint rule for `.ts`/`.tsx` files (the hand-written `eslint.config.js` declared only 4 manual globals — `window`/`document`/`fetch`/`console` — so `eslint` failed with 25 `no-undef` false positives for ambient DOM/JSX types and Jest globals: `JSX`, `HTMLButtonElement`, `RequestInit`, `RequestInfo`, `Response`, `URL`, `global`, `jest`, `expect`).
**Context:** These files were part of an already-built-but-uncommitted frontend scaffold from a prior session (see PROGRESS.md's Phase 12 entry) — this phase's job was to finish and land it, not silently paper over what didn't actually run.
**Rationale:** `--legacy-peer-deps`/`--force` would have masked the real incompatibility instead of fixing it, and could produce a broken install later. Disabling `no-undef` for TypeScript files is typescript-eslint's own documented recommendation (`tsc --noEmit`, run separately via `npm run typecheck`, already fully covers undefined-symbol errors with correct knowledge of ambient types that ESLint's own rule doesn't have) — not a blanket "turn off a rule that's inconvenient," and confirmed by running `tsc --noEmit` clean both before and after the ESLint change.
**Affects:** `package.json` (`eslint-plugin-react-hooks` version, added `@types/jest-axe`), `eslint.config.js`.

**Decision:** The 28 `npm audit` advisories (1 moderate, 27 high) surfaced after the first successful `npm install` are left unfixed this phase.
**Context:** They live entirely in transitive dependencies of dev-only tooling (eslint/jest/vite's own dependency trees) — nothing in `dependencies` (only `react`/`react-dom`) is implicated, and none of this ships to a real WordPress install; it's build/test tooling only.
**Rationale:** `npm audit fix --force` can silently jump major versions of eslint/jest/vite and break the just-verified-working lint/typecheck/test/build pipeline in ways that would need their own dedicated verification pass — out of scope for "finish and land the already-built Phase 12 admin shell." Deferred to a dedicated dependency-hardening pass rather than risked blind.
**Affects:** `package-lock.json`. Revisit in a phase whose actual scope is dependency maintenance.

## Phase 13 — Monitoring + Reporting Engines — 2026-07-25

**Decision:** `MonitoringService` has no `Core/Scheduler.php`-driven automatic trigger. `start()` arms monitoring and takes a baseline snapshot; `scan()` — called manually via `POST /monitoring/scan` — is the actual Resource Scanner/Change Detection/Validation/Notification pipeline docs/20's own diagram describes as happening on a Scheduler tick.
**Context:** `Core/Scheduler.php` is listed in every prior phase's "explicitly still missing" section going back to Phase 2 — no cron/queue infrastructure exists yet to call anything periodically.
**Rationale:** Building a fake in-process "scheduler" (e.g., a loop with `sleep()`) inside a single PHP request would be actively wrong for a WordPress plugin (no long-running processes), and a real one needs `wp_schedule_event`/Action Scheduler-style infrastructure this project hasn't built. Exposing the detection pipeline as a real, manually-triggerable REST action is honest about what exists today and gives a future Scheduler phase something concrete to call — not a redesign once it exists.
**Affects:** `app/Services/MonitoringService.php`, `app/Http/Controllers/MonitoringController.php` (`POST /monitoring/scan`, not in docs/20's own REST list — same precedent as Phase 6's `/generation/rollback`).

**Decision:** A resource's "fingerprint" for change detection combines its Discovery Map metadata (type/location/status/version/health/dependencies — explicitly excluding `lastChecked`) with its generated content when a Generator is registered for its module (`GenerationService::has($resource->module)`), hashed with `sha256`.
**Context:** `DiscoveredResource::lastChecked` is set to `gmdate('c')` on every `discover()` call (confirmed in `RobotsModule::discover()`), so including it in the fingerprint would make every single resource look "changed" on every scan — a false positive on 100% of resources, not a real signal.
**Rationale:** Excluding the one field that's guaranteed to differ on every call, while including everything that actually describes the resource's state (plus its real generated content, when available, so a content-level change — e.g., a future Settings-driven robots.txt edit — is caught too, not just metadata) is the honest, minimal definition of "did this resource change." Reuses Discovery + Generation's existing output rather than inventing a new scanning mechanism.
**Affects:** `app/Services/MonitoringService.php::fingerprint()`.

**Decision:** `ChangeType` has only three cases (Created, Modified, Deleted) of docs/20-Monitoring-Engine.md's nine (also Broken, Deprecated, Moved, Redirected, Disabled, Expired); `NotificationPriority` has all five documented cases (Critical, High, Medium, Low, Informational) but `MonitoringService` only ever produces three of them (Critical, Medium, Informational).
**Context:** Broken/Redirected/Expired need live HTTP-following and SSL-certificate checks; Deprecated/Disabled need a Standard lifecycle state this project doesn't model; Moved needs location history a single fingerprint-per-resource-id snapshot doesn't retain. High/Low priority has no signal to derive from — `ValidationResult` (Phase 5) carries a pass/warn/fail status, not a severity, the exact same limitation `ScoringService` already documented for its own weighting (Phase 7) and `Recommendation::priority` inherited (Phase 10).
**Rationale:** Same precedent as `ScanType` (Phase 9): include every documented value that's a real, well-defined concept so the enum's shape doesn't need to change later, but don't fabricate detection logic for values nothing in this codebase can yet distinguish.
**Affects:** `app/DTO/ChangeType.php`, `app/DTO/NotificationPriority.php`.

**Decision:** `ExportFormat` has only two cases (Json, Markdown) of docs/21-Reporting-Engine.md's eight (also PDF, HTML, CSV, Excel, XML, ZIP).
**Context:** PDF/Excel/ZIP need an external library this project doesn't depend on; CSV/XML/HTML are plausible but have no real consumer driving a specific shape decision yet.
**Rationale:** Both implemented formats are genuinely complete (JSON is the report's own `toArray()`; Markdown is a real, readable multi-section rendering, not a stub), comfortably satisfying the Phase 13 exit criterion's "exports in at least one format." Adding more formats without a concrete need would be scope creep, not phase completion.
**Affects:** `app/DTO/ExportFormat.php`, `app/Services/ReportService.php::export()`.

**Decision:** `ReportService::generate()` builds docs/21's own "Technical Report" shape only (an `AuditReport` + `Recommendation`s + `MonitoringEvent`s) — not Executive, Agency, White Label, Compliance, Security, or Performance report types, all separately named in docs/21.
**Context:** Those report types need content this project has no source for: business-summary copywriting ("Business Summary," "Estimated Growth"), client/agency branding (logo, colors, custom footer), and compliance-framework mappings — none of which any engine built so far produces.
**Rationale:** Fabricating placeholder branding fields or invented business-impact estimates would be exactly the kind of placeholder data CLAUDE.md prohibits. The Technical Report is the one type built entirely from real, already-computed data (Audit/Recommendation/Monitoring), so it's the only one built this phase.
**Affects:** `app/DTO/Report.php`, `app/Services/ReportService.php`. No `ReportType` enum was introduced at all, since only one type exists — same "don't build an abstraction with one real implementation" discipline as Phase 7's Scoring decision.

**Decision:** `/monitoring/history` and `/reports/history`/`/reports/templates`/`/reports/share`/`DELETE /reports/cache` (all named in docs/20's and docs/21's own REST API lists) are not implemented.
**Context:** `/monitoring/history` would need to differ from `/monitoring/events` to be worth a separate route, which requires persisted storage (richer previous/current-value history) that doesn't exist — returning the identical in-memory list under two names would be a fake distinction. The four `/reports/*` routes need multi-report persisted storage, a report-template system, external sharing/delivery, and a caching layer, none of which exist.
**Rationale:** Same discipline as every prior phase's route omissions (Phase 9's `/audit/fix`/`/audit/verify`, Phase 10's `/autofix/batch`/`/autofix/history`) — build the routes real data supports, document the rest as deferred rather than stubbing them out.
**Affects:** `app/Http/Controllers/MonitoringController.php`, `app/Http/Controllers/ReportController.php`, `routes/api.php`.

## Phase 14 — AI-native modules (MCP, Agent Skills, API Catalog, OAuth Discovery) — 2026-07-26

**Decision:** `McpModule::generate()` publishes a real Server Card identity (name/description/organization/version) but every `capabilities`/`resources`/`tools`/`prompts` field is hardcoded `false`/empty, not the `true`/populated values docs/12-MCP-Spec.md's own "CAPABILITIES" list (Resources, Tools, Prompts, Sampling, Streaming, Completion, ...) would imply a fully-featured MCP server has.
**Context:** Those capabilities describe a live MCP JSON-RPC transport — a server that actually serves resources, executes tools, and streams completions. This project has never built one; only this Server Card discovery/identity layer exists.
**Rationale:** Declaring `"tools": true` (or populating a `tools` array) when no tool-execution engine exists would describe infrastructure that doesn't exist — exactly the fabricated capability/mock production data CLAUDE.md prohibits, and arguably worse than most mock data since an AI agent that discovers this card would actually attempt to call a tool that isn't there. Honest empty/false values are the correct choice until a real MCP transport phase is built.
**Affects:** `app/Modules/Mcp/McpModule.php`. A future phase adding a real MCP transport should flip these to real values as each capability is actually implemented, not before.

**Decision:** `AgentSkillsModule`'s Skill Registry publishes exactly three skills — this plugin's own real, already-working `GET /score`, `POST /audit/start`, `GET /recommendations` REST actions — not docs/13-Agent-Skills-Spec.md's own "DEFAULT SKILLS" example list (Book Appointment, Find Doctor, Open WhatsApp, Download Brochure, ...).
**Context:** Those example skills describe capabilities specific to a medical/business site with booking, WhatsApp, and file-download infrastructure — none of which any WordPress site has by default and none of which this plugin builds.
**Rationale:** Publishing fabricated skills that don't actually work if an agent tried to invoke them would be worse than publishing none — the whole point of a Skill Registry (per docs' own Vision: "AI agents should understand what a website can DO") is that every listed skill is genuinely callable. This plugin's own real REST actions are genuinely callable today, so those are what's published — real dogfooding of this project's own existing REST surface, not fabricated example data.
**Affects:** `app/Modules/AgentSkills/AgentSkillsModule.php`. Real per-skill CRUD (`POST /skills`, `DELETE /skills/{id}`) is deferred until skills need to be user-defined rather than fixed to this plugin's own actions.

**Decision:** `ApiCatalogModule::generate()` builds its catalog from a static, hand-maintained PHP constant listing every route this plugin registers in `routes/api.php` (83 as of this phase), rather than a live `rest_get_server()->get_routes()` introspection at generate()-time.
**Context:** docs/05-Modules.md's own responsibility for this module is "Scan REST API" — a live WordPress function call would satisfy that literally and never drift out of sync with reality.
**Rationale:** Every other Generator in this codebase (Robots' rule set, Headers' header list, LLMS' content, MCP's server card, Agent Skills' registry) is a pure function with no WordPress runtime dependency — introducing the one exception here for API Catalog specifically would make it a special case for a benefit (auto-sync) this project's small, single-maintainer route file doesn't yet need enough to justify the added complexity (stubbing a `WP_REST_Server`-shaped fake in every test that touches this Generator). The static list is genuinely real data (every route in it truly exists — cross-checked against `ApiRoutesTest`'s own independently-computed expected-route list via a new regression assertion), just one that must be updated by hand when routes change — a documented, known limitation, not a fabrication.
**Affects:** `app/Modules/ApiCatalog/ApiCatalogModule.php`, `tests/Unit/Routes/ApiRoutesTest.php` (added a count cross-check between the two independently-maintained lists). Revisit with live introspection if this list drifts out of sync in practice.

**Decision:** `OAuthDiscoveryModule` implements `ModuleInterface`/`DiscoveryInterface`/`ValidatorInterface` but *not* `GeneratorInterface` — unlike every other module in this codebase. Three small, dedicated Generator classes (`OpenIdConfigurationGenerator`, `OAuthAuthorizationServerGenerator`, `OAuthProtectedResourceGenerator`) exist instead, each registered under its own id in `GenerationService`, all constructor-injected into the Module so its `validate()` can dispatch to whichever one a given resource corresponds to.
**Context:** Per ADR-001/`.project/09-Canonical-Architecture.md`'s ownership table, OAuth Discovery is the one module owning three Standards (`openid-configuration`, `oauth-authorization-server`, `oauth-protected-resource`) instead of the usual one. `GenerationService::registerGenerator(string $id, GeneratorInterface $generator)` maps exactly one id to one generator instance whose `generate()` returns exactly one string — one object can't honestly serve three different generator/resource ids with three different outputs under that contract.
**Rationale:** Building three tiny, single-purpose Generator classes (each under 40 lines) is a smaller, more honest deviation than either (a) forcing three separate near-duplicate Module classes sharing one `id()`/lifecycle identity, which ADR-001's ownership table treats as one module, or (b) making `GeneratorInterface::generate()` return an id-keyed array, which would change the contract for every other module's simple single-output case. Each Standard (`OpenIdConfigurationStandard` etc.) holds a reference to its own specific Generator directly for `generate()`/`supports()`, while `discover()`/`validate()` still delegate to the shared owning Module — the smallest change to the established delegation shape that a three-Standards-one-Module case actually requires.
**Affects:** `app/Modules/OAuthDiscovery/*` (7 files instead of the usual 3-4), `app/Http/Controllers/OAuthDiscoveryController.php` (combined overview) + `OAuthDiscoveryFileController.php` (instantiated three times in `routes/api.php`, one per document).

**Decision:** `OpenIdConfigurationGenerator` and `OAuthAuthorizationServerGenerator` publish a real `issuer` (from `home_url('/')`) plus explicit `null` values for `authorization_endpoint`/`token_endpoint`/`jwks_uri` and a plain-language "not configured" note — not a spec-compliant OpenID Connect Discovery 1.0 / RFC 8414 document, which mandates those endpoint fields.
**Context:** Neither WordPress core nor this plugin implements an actual OAuth 2.0 Authorization Server or OpenID Provider (no `/oauth/authorize`, `/oauth/token` routes exist anywhere in this codebase) — this was checked explicitly, not assumed, before writing these generators.
**Rationale:** Inventing endpoint URLs for infrastructure that doesn't exist would be exactly the fabricated capability data CLAUDE.md prohibits — and arguably worse than ordinary mock data, since a real OAuth/OIDC client that discovers these documents would attempt an actual authorization flow against fake endpoints and fail in a confusing way. A strict OIDC/RFC 8414 client correctly failing to treat this site as a real Provider/Authorization Server (because the required fields are honestly absent) is the correct outcome, not a bug to work around. `oauth-protected-resource` (RFC 9728) is the one document of the three built fully spec-compliant, since its only mandatory field is `resource` and everything else is genuinely optional — so publishing it accurately doesn't require fabricating anything.
**Affects:** `app/Modules/OAuthDiscovery/OpenIdConfigurationGenerator.php`, `app/Modules/OAuthDiscovery/OAuthAuthorizationServerGenerator.php`. Revisit only if a real OAuth Authorization Server / OpenID Provider is ever built as its own, much larger phase — not by silently filling in these fields.

**Decision:** Added `Functions\when('home_url')`/`Functions\when('rest_url')` stubs to the existing `RobotsScoringEndToEndTest` (Phase 8), which construct the real `Plugin` and run every registered validator against every discovered resource.
**Context:** Adding `OAuthDiscoveryModule`'s validator (which calls its Generators' real `home_url()`/`rest_url()`) caused this one pre-existing test to fail with "Call to undefined function" — the first time any registered validator/generator in this codebase called a WordPress runtime function reachable from that full-system test path.
**Rationale:** Confirms (rather than merely assumes) that `ValidationService::validate($resource)` really does run every registered validator against every resource system-wide, a pre-existing architectural characteristic from Phase 5 — not something this phase introduced. Fixing the one real test that exercises this path, rather than avoiding WordPress function calls in these Generators altogether, was the right trade: `home_url()`/`rest_url()` are exactly the real, honest values these two documents' `issuer`/`resource` fields need to be meaningful at all.
**Affects:** `tests/Unit/EndToEnd/RobotsScoringEndToEndTest.php`.

## Phase 15 — Remaining modules + hardening — 2026-07-26

**Decision:** Commerce/Analytics/License/Updater each report one real, currently-true fact (WooCommerce presence, zero traffic counts, free/unactivated tier, current version on the stable channel) rather than any of the enterprise-scale capability docs/05-Modules.md briefly sketches for them (x402 machine payments, AI usage charts, subscription/agency/enterprise license tiers, beta/nightly update channels with rollback).
**Context:** All four get only a few lines in docs/05-Modules.md with no dedicated spec doc (`.project/04-Questions.md` Q7 already flagged these as "lower-priority... confirm"), and per ADR-001's ownership table none of them own a Standard — they're platform-support features, not AI-standard modules.
**Rationale:** Same discipline every module in this project has followed: real, honest, currently-true content over fabricated capability. Commerce/License/Updater in particular touch payments, licensing, and update delivery — domains where fabricated data isn't just "mock data," it's the kind of claim a real integration (a payment processor, a license-check script, an update client) could actually act on incorrectly. Reporting real zero/false/free/stable values is safe; reporting fabricated "true"/"pro"/"beta available" values would not be.
**Affects:** `app/Modules/{Commerce,Analytics,License,Updater}/*Module.php`.

**Decision:** `.github/workflows/ci.yml` runs PHP quality (4-version matrix) + frontend quality + a packaging/verification job — and deliberately does **not** include the real-WordPress-install, MySQL-version, multisite, WooCommerce/Elementor-compatibility, or browser-test jobs docs/28-Testing-Strategy.md's own "CI MATRIX"/"Release Candidate Testing" sections describe.
**Context:** Those jobs need infrastructure this project has never built: a WordPress core test-suite bootstrap (`bin/install-wp-tests.sh`-style scaffolding + a real MySQL service), `tests/Integration` had exactly 0 tests before this phase, and no browser-automation tooling exists anywhere in this repo.
**Rationale:** A CI workflow referencing test suites, WordPress installations, or browser jobs that don't actually exist would be permanently red (or silently no-op) on every push — actively misleading about what "CI matrix" means for this project, and arguably worse than not claiming one. The three jobs that were built are real, run today, and pass for real. The gap is documented explicitly (here and in `.project/RELEASE-GATE-CHECKLIST.md`) rather than papered over with an aspirational-looking workflow file that doesn't do what it claims.
**Affects:** `.github/workflows/ci.yml`. Extending this to a real WordPress/multisite/compatibility matrix is real, valuable future work — once the test-suite bootstrap it depends on is built.

**Decision:** `Plugin::activate(bool $networkWide = false)` iterates `get_sites()` + `switch_to_blog()`/`restore_current_blog()` only when `$networkWide && is_multisite()`; single-site and per-site (non-network-wide) activation are unchanged.
**Context:** WordPress's `register_activation_hook()` callback automatically receives a `$network_wide` boolean as its first argument (core behavior, not something this project added) when a super admin network-activates a plugin — but WordPress itself does *not* iterate every site's context for the plugin; that's the plugin's own responsibility if it wants every site provisioned.
**Rationale:** Before this fix, network-activating this plugin across, say, 50 sites would have set `oxy_ai_installed_at`/`oxy_ai_version` options on only whichever single site happened to be "current" during that one admin request — the other 49 sites would silently never get them, since `get_option()`/`update_option()` operate on the WordPress-determined "current" blog's options table. This is exactly the kind of multisite gap docs/28's own Supported Environment Matrix ("Network Activated") calls out, and one this project had never actually verified until this phase's audit found it.
**Affects:** `app/Core/Plugin.php` (`activate()`, new private `activateCurrentSite()`, `deactivate()` signature). `wpmu_new_blog`/`wp_initialize_site` (a site created *after* network activation) is a separate, still-open gap — not fixed this phase, logged in PROGRESS.md's "explicitly out of scope" list.

**Decision:** `bin/build-release.sh` stages only `app/`, `routes/`, `dist/`, `oxy-ai-readiness.php`, `uninstall.php`, `composer.json` — no `tests/`, `.project/`, `docs/`, `assets/react/` (source), config files, or `.git*` — and runs its own `composer install --no-dev` inside that staged copy rather than reusing the working tree's own `vendor/`.
**Context:** docs/28-Testing-Strategy.md's own "PACKAGE TESTING" checklist requires "Development Files Excluded" and "Vendor Dependencies Included" (production only) as two separate, real requirements.
**Rationale:** Staging a fresh copy and running a scoped `composer install --no-dev` there (rather than filtering the existing `vendor/`, which mixes dev and prod packages) is the only way to guarantee no dev-only package leaks in — verified mechanically by `tests/Integration/PackagingTest.php` rather than assumed. This project currently has zero runtime Composer packages (everything in `require-dev` is lint/test tooling), so the staged `vendor/` correctly ends up as just the autoloader.
**Affects:** `bin/build-release.sh`, `tests/Integration/PackagingTest.php`, `.github/workflows/ci.yml` (`package` job).

**Decision:** Fixed `composer.json`'s `test`/`test:coverage` scripts to explicitly scope to `--testsuite=Unit` (previously unscoped, silently running every configured testsuite); added the missing `--no-coverage` flag to `test:integration`.
**Context:** `phpunit.xml.dist` has always declared both `Unit` and `Integration` testsuites, but `composer test` never named one explicitly — invisible while `tests/Integration` was empty (0 tests either way), but the moment this phase added real integration tests, `composer test`/`composer quality` silently started also running them, depending on `dist/` existing and spawning `composer install` subprocesses — exactly the kind of environmental dependency the fast, hermetic Unit gate was never supposed to have.
**Rationale:** This is a real, pre-existing latent defect this phase's own packaging work happened to surface, not a design choice being second-guessed — fixing it restores `composer test`'s documented-everywhere-in-this-project behavior ("fast, no-DB, no-WordPress-bootstrap unit gate") rather than leaving Unit and Integration accidentally coupled.
**Affects:** `composer.json`.
